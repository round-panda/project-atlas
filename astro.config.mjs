import { defineConfig } from 'astro/config';

export default defineConfig({
  // Set site when you know the final URL, e.g.:
  // site: 'https://projectatlas.tcusd.org',
  site: 'https://round-panda.github.io',
  base: '/project-atlas',
});
